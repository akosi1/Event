<IfModule mod_rewrite.c>
    RewriteEngine On

    # Force HTTPS (only if not already HTTPS)
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

    # Let Laravel handle routing (no rewrite to public folder needed)
</IfModule>